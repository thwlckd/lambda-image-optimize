{
  "name": "lambda-image-optimize",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "prebuild": "npm run clean-install-sharp && rm -rf dist",
    "build": "esbuild index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js --external:sharp",
    "postbuild": "npm run sharp-compat && cp -r node_modules dist/ && cd dist && zip -r index.zip index.js node_modules",
    "clean-install-sharp": "npm uninstall sharp && npm install sharp@0.32.6",
    "sharp-compat": "npm uninstall sharp && npm install --platform=linux --arch=x64 sharp@0.32.6",
    "test": "npm run clean-install-sharp && vitest"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.876.0",
    "sharp": "^0.32.6",
    "stream": "^0.0.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.3.0",
    "esbuild": "^0.25.9",
    "prettier": "^3.6.2",
    "vitest": "^3.2.4"
  }
}
